
CREATE TABLE Pokemon (
  key INTEGER NOT NULL PRIMARY KEY,
  name TEXT NOT NULL,
  url TEXT NOT NULL
);

insertItem:
INSERT OR REPLACE INTO Pokemon (key, name, url) VALUES (?, ?, ?);

selectAll:
SELECT * FROM Pokemon ORDER BY key;

deleteAll:
DELETE FROM Pokemon;

count:
SELECT COUNT(*) FROM Pokemon;